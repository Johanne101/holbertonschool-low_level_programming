#include "holberton.h"
/**
 * mul - This will multiply two integers
 * @a: Character 'a' is the first number,
 * @b: Character 'b' is the second number.
 * Return: The result of a * b.
 */
int mul(int a, int b)
{
	int c;
	(c = a * b);
	return (c);
}
